# Kiddo Assist API — base requirements (Iteration 0) + KB data layer (Iteration 1).
# Later iterations add the ML stack (faster-whisper, transformers, kokoro, bge-m3...).
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
# Vector/hybrid content store (Iteration 1: data layer).
lancedb>=0.13
pyarrow>=16.0

# --- RAG (Iteration 3) -------------------------------------------------------
# Real bge-m3 embedding + bge-reranker-base need `torch` + `FlagEmbedding`.
# They are NOT declared here yet on purpose: the compose api image stays slim and
# RAG degrades to vector retrieval (or ungrounded chat) without them. The dev
# seed/verify path runs on the host, where torch + FlagEmbedding are installed.
# Iteration 17 (deployment) finalizes this ML stack for the compose image.
#   # torch>=2.2      (CPU wheel, platform-specific)
#   # FlagEmbedding>=1.2