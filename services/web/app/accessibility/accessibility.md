# Accessibility — Phase 7 (§6.3, §8)
# Access is a value, not a feature.

ACCESSIBILITY_ITEMS = {
    "aria_labels_mic": True,
    "aria_labels_send": True,
    "aria_labels_video_controls": True,
    "aria_live_region_for_answer": True,
    "text_size_toggle": True,
    "high_contrast_mode": True,
    "visible_focus_rings": True,
    "keyboard_accessible": True,
    "switch_accessible": True,
    "large_tap_targets": True,
    "pre_reader_friendly_listening_feedback": True,
}
